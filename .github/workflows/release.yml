name: Release

on:
  workflow_dispatch:

jobs:
  placeholder:
    name: Manual Release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Info
        run: echo "Release workflow - trigger manually when ready"
